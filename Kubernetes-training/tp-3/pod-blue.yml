apiVersion: v1
kind: Pod
metadata:
  name: blue-pod
  namespace: production
  labels:
    app: web
spec:
  containers:
    - name: webapp-color-container
      image: kodekloud/webapp-color
      env:
        - name: APP_COLOR
          value: "blue"
      ports:
        - containerPort: 8080